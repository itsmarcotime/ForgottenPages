    {{#if characters.length}}
<section class="container dashboard characterBox justify-content-center">
    <ul>
        {{#each characters as |character|}}
        <li>
            <a href="/dashboard/character/{{character.id}}"> {{> character character}}</a>
        </li>
        {{/each}}
    </ul>
</section>
    {{/if}}